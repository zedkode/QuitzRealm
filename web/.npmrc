# The lockfile is intentionally refreshed during dependency upgrades; accept recent
# releases after local validation instead of rejecting them during cloud install.
minimum-release-age=0
minimumReleaseAge=0
